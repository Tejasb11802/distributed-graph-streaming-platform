apiVersion: v1
kind: Service
metadata:
  name: neo4j-service
spec:
  type: ClusterIP
  selector:
    helm.neo4j.com/instance: my-neo4j-release
  ports:
    - name: neo4j-http
      port: 7474
      targetPort: 7474
    - name: neo4j-bolt
      port: 7687
      targetPort: 7687
